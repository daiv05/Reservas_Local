@model IEnumerable<Reservas_Local.Models.Hora>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HInicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HFin)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HInicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HFin)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HoraID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HoraID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HoraID })
        </td>
    </tr>
}

</table>
